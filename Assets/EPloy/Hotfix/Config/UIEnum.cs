
using UnityEngine;

namespace EPloy
{
    public enum UIName
    {
        Default = 0,
        StartForm = 1,
        TipsForm = 2,
        LoginForm=3,
        LoadingForm = 4,
    }

    public enum UIGroupName
    {
        Default = 0,
        Level1=1,
    }
}
