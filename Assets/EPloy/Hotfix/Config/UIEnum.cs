
using UnityEngine;

namespace EPloy
{
    public enum UIName
    {
        Default = 0,
        StartForm = 1,
        TipsForm = 2,
    }

    public enum UIGroupName
    {
        Default = 0,
    }
}
